---
title: Whose Ball v2 — Verified Pilot Requirements Delta
type: feat
status: active
date: 2026-07-12
origin: docs/plans/2026-06-29-feat-whose-ball-cohort-pm-plan.md
supersedes-target: c2w1pm-submission (that contest closed Fri Jul 3; new target is the pilot's Project 1)
---

# Whose Ball v2 — the verified requirements delta

The June 29 design targeted the summer Cohort 2 Week 1 contest, which has already run. The live target is **Project 1 (PM platform) of the pilot George applied to** (admissions PR #22, decision ~Jul 14). This doc records what the program's own canon actually requires, verified 2026-07-12 against the public `rogerSuperBuilderAlpha/hult-cohort-program` repo, and what whose-ball must become to compete.

## Build-state cursor

> **Step:** SESSION D IN PROGRESS (structure locked 2026-07-13, all four on recommendation): (1) deploy FIRST, then features land on prod; (2) momentum feed + weekly narrative run on app events (tasks done, stage advances, reviews filed, blockers raised/cleared), GitHub merged-PR ingest designed-in but stubbed until real submission branches exist; (3) journey header = current phase + live countdown + YOUR standing, on every page; full spine only on the momentum page; (4) README thesis-led (motivation thesis → six decisions as feature tour → setup/architecture/URL/limitations tail). Resume at the first unfinished block: ~~deploy~~ → ~~momentum page~~ → ~~journey header~~ → ~~Assists + Possession view~~ → ~~Rally~~ → ~~push to prod + verify~~ (**Friday trigger MET 2026-07-15**: Rally+Assists verified on prod two days early, all 6 routes 200, strip/spine/tiles live) → ~~dead-ball whistle with cause-typed pickup + routed remedies~~ (DONE 2026-07-16, `066b303`, verified on prod — see WHISTLE DONE below) → ~~weekly narrative~~ (DONE 2026-07-16, `b65949b`, structure locked in serial — see NARRATIVE DONE below) → ~~README+AGENTS.md~~ (DONE 2026-07-18, `806257d` — README thesis-led per structure lock #4 with the ratified Federer epigraph named and quoted; six-decision feature tour folds the second-harvest mechanics into their homes; tail = sign-in doors / fresh-clone setup / ASCII architecture / known limitations, per the program's README requirements. AGENTS.md created as the program requires: commands, layout, 7 hard invariants, the drizzle-kit push hazard, the fixture-driven verification pattern, current data caveats. `.env.local.example` rewritten to v2 truth (DATABASE_URL + Auth.js + PAT; Upstash/Anthropic vars gone) and the two vestigial deps `@anthropic-ai/sdk` + `@upstash/redis` uninstalled — zero usages confirmed by grep; `npm run build` green, all routes. NOT prod-verified beyond build: docs-only + dep removal, Vercel auto-deploy assumed; the 10-user smoke covers it) → ~~10-user smoke~~ (DONE 2026-07-18: fresh clone → npm install → all 6 migrations applied in order to a disposable `whose_ball_smoke` DB on the same Neon project → build green → `npm start` → 10 concurrent simulated users driving the REAL forms via progressive-enhancement POSTs (signup + create-project + 18 page loads each): 200/200 requests OK in ~5s, DB exactly 10 users / 10 projects / 10 project_created events, all 10 sessions real (cookie-verified, authenticated /me), zero server errors; smoke DB force-dropped after. FINDING: self-hosted production (`npm start` off Vercel) needs `AUTH_TRUST_HOST=true` (Auth.js UntrustedHost 500s on signup without it); `npm run dev` (the README flow) and Vercel are unaffected; documented as a comment in `.env.local.example`. HAZARD LOG: BSD sed's lack of `\|` alternation caused the first migration attempt to target the PROD URL unmodified — no harm (first CREATE TYPE failed on already-exists, nothing else ran), and the migration/wipe/drop scripts now hard-refuse any URL whose path isn't `whose_ball_smoke`) → **submission PR** (accelerated by Roger's Fri Jul 18 nudge email: PR opens TODAY, not Sat; per the email + merged-PR precedent #54/#57, the branch adds ONE file `submissions/gge513-project-1.md` (repo link + prod URL + one-paragraph pitch) via a FORK of `rogerSuperBuilderAlpha/hult-cohort-program`, PR body carries the six sections: production URL · fresh-clone-verified setup · architecture · motivation notes · known limitations · agent usage) → **SUBMITTED 2026-07-18: PR #59 OPEN** (https://github.com/rogerSuperBuilderAlpha/hult-cohort-program/pull/59, verified: correct base/head/title, one file, MERGEABLE; fork `gge513/hult-cohort-program`, local clone `~/cursor-course/hult-cohort-program`, branch tracks the fork so future pushes auto-update the PR). → **MERGED + STAFF REVIEW IN (2026-07-18 22:25 UTC, comment on #59): verdict BALLOT-ELIGIBLE.** All 7 baseline requirements pass (the ≥30-accounts bar read as system capacity, the same standard as other merged submissions — open signup + OAuth + the 10-user smoke clear it); staff called it "the cleanest codebase in the batch"; rubric Production 4 · Core 4 · Code 5 · Ecosystem 5 · UX 4. → **STAFF ASKS CLOSED (2026-07-19, comment posted on #59: issuecomment-5013529695):** (1) accounts = open signup + create-on-request confirmed (deliberate call: no synthetic seeding — it would recreate the fake-user problem); (2) demo purge DONE and live-verified — deleted 4 users + 4 submissions + 3 `submission_merged` events (live count was 3, not the noted 4 — Sam never had one) + George's seeded demo review + vote (FK-chained to fake-Maya's submission; disclosed to staff); prod DB now = George alone, 1 project, 4 real events, all routes 200, zero fake names in the feed. (3) `github-reviews.ts` matcher SHIPPED (`06f8697`, auto-deployed): live canon check found the CURRENT title format is `Review by @{you}: @{peer}` (winner-selection.md on origin/main) and staff really file suffixed titles like `(phase-1-project-3)` — new matcher = prefix + handle-boundary + 3-page pagination, live-fire verified against all three formats in the wild (course-repo issues #18/#20 + a created-then-closed test issue gge513/whose-ball#1). **VOTE CANON CHANGED (verified on origin/main): official vote = public optional `Vote: up` line in the review issue BODY on the peer's repo; no downvotes; the platform paste-URL + private-ballot design is retired — whose-ball's in-app ballot is now a private tracker only (tune-list #9: truth-telling console copy + parse issue body for `Vote: up` → "official vote detected").** **BOTH GATING ITEMS SHIPPED (2026-07-19 late night):** (a) ROSTER INGEST DONE — `scripts/ingest-roster.mjs` (`5bef901`): source of truth = merged PRs on upstream branch `projects/summer26/phase-1-project-1` (NOT main — the repo restructured; participant submissions never land on main; the local course-repo clone's `origin` is the FORK, upstream remote added), author login + real merged_at, URLs parsed from submission files with vendored-folder README descent + fork-sync-mess fallback via branch listing; idempotent (users ON CONFLICT DO NOTHING so OAuth claims rows; submissions upsert; events dedupe). Ran against prod: 18 PRs → 15 participants, 15/15 parsed, +14 users +15 submission_merged events with real timestamps, live-verified on the feed. **RERUN after Sun 17:00 ET merge deadline** (only 15 of ~65 have merged; 3 vendored-app submissions point at their course-repo folder as repo_url until they fix their files — rerun refreshes). (b) TUNE-LIST #9 DONE (`e1da7e2`): ballot = honest private tracker; both filing paths parse the issue body and auto-sync a confirmed `Vote: up` (absence/unreadable never clears — official abstain is legitimate); console copy states the vote of record; bodyHasVoteUp + issueHasVoteUp live-verified both paths on a real issue. **#7 SHIPPED (`56080e8`): MemberLink component, names are doors on /tasks, /projects, project page, /review, /me catch card; /me headline links "your season →"; tasks row + project card de-nested (anchors don't nest) so titles keep the project door.** **TUNE-LIST DISPOSITION COMPLETE (2026-07-19 afternoon, `6c3fc03`, all items George's serial calls):** #1/#3 every /me card leads with the project name + mechanic badge, possession cards de-nested (waiter names now member links — the #7 exception CLOSED); #2 one ball-existence test (holder, same as the whistle sweep) + setBallAction enforces action+holder server-side + whistle badge in the blank-action branch; #4 blocker acknowledgment PARKED post-pilot (George's call: light-blocker model may be right anyway; logged in README known limitations); #5 define-gate refusal shows the contract (three questions ✓/○) in a popover at the button; #6 (George picked reverse-gear + two-tap over evidence gates): retreatStageAction one station back, silent — corrections aren't story — plus two-tap advance (arm, 4s disarm, commit), so the 3-click define→verify accident class is dead; #8 connective tissue is its own paragraph (writeChapter returns paragraphs[]). README stale lines trued (demo rows purged, matcher shipped). VERIFIED fixture-driven on the sandbox: 29 real-form multipart-POST checks (guard refusals + legit edit lands, silent retreat vs event-emitting advance, crafted-POST past an empty gate refused, card leads/badges/links, two-paragraph chapters) + browser click-through of two-tap arm/disarm/submit and the locked-gate popover; sandbox restored to George's hands-on state after (still up, drop when done). NOTE for future form-driving: server-action forms POST as multipart/form-data (urlencoded silently no-ops) and Next server-renders text+expr with `<!-- -->` separators — strip comments before text assertions. Remaining: George's whistle clear on /me (prod); the Sunday-night ingest rerun after the 17:00 ET merge deadline; then the review+vote sprint (58 by Mon 14:00 ET).** Then: George can clear his own whistle on /me by linking PR #59 as the evidence (his call, his ball). Spec for the new blocks: the Second harvest section below + `docs/brainstorms/2026-07-15-evernote-pm-communication-ore.md`. (Session D part 1 closed 2026-07-13, COURSE-LOG Entry 005; part 2 closed 2026-07-15, Entry 006 filed — journey header `75dc775`, assists+possession `f4409f7`, rally `c813af9`, all verified locally end-to-end via fixture-driven real forms and live on prod. Whistle closed 2026-07-16, Entry 007.)
>
> **SANDBOX TUNE-LIST (2026-07-18/19 hands-on session — George drove every mechanic on a seeded `whose_ball_sandbox` DB + localhost dev; sandbox still up, drop when done):** #1+#3 /me cards lead with person/mechanic and bury the project name in 11px faint — RULE: every command-center card leads with the project name, mechanic becomes the badge (George twice failed to connect a card to its work). #2 ball-existence test is inconsistent (project page keys on `nextAction`, /me on `ballHolderId`) and `setBallAction` doesn't enforce holder⇒action server-side; whistle badge invisible in the no-action branch. #4 blockers: unblocker has no "seen it / on it" acknowledgment — open design question vs the light-blocker model, cohort-scale matters. #5 Define-gate refusal renders too far from the stage control — move the contract preview next to the button (George: liked the three-question level of "defined"). #6 BIGGEST: stages have no reverse gear and no friction past the define gate — George accidentally advanced define→verify in 3 clicks, each a public feed line; wants stage-back + explore per-stage evidence gates (candidate: build needs ball set, verify needs a task done, ship needs evidence link — honors "gates the story, never the work"); cheap alt: collapse rapid multi-advances into one feed line. #7 member pages (the season narrative!) reachable ONLY via /momentum actor links — names must be links EVERYWHERE + /me gets "your season →" (George: "two dead name-clicks taught me names aren't links here"). #8 narrative: split connective-tissue movement into its own paragraph (amends the one-paragraph-per-chapter call locked 07-16; George ratified after seeing a real week rendered — verdict "read like my week"). #9 vote console models the retired private-ballot canon (see VOTE CANON CHANGED above). Fixed during the session: the github-reviews matcher (#59 staff flag).**
>
> **NARRATIVE DONE (2026-07-16, commit `b65949b`, writer unit-read across all 11 shapes + 9/9 e2e checks on the live page, fixtures deleted):** four structural calls locked in serial: **composed match report** (one paragraph per chapter, sentence rules in code — deterministic template composition, NO LLM at runtime: facts about real people, 58 fresh-clone reviewers, no key dependency; the retired v1 Anthropic module and a once-per-chapter cached LLM pass stay on the post-pilot roadmap next to the season table); **chapters = the pilot's real weeks** off a SEASON_LABELS list ("Build week", "Review week"), with the ratified fallback that undated weeks still narrate as anonymous Mon–Sun weeks — feeding the app its next season is one small edit, post-pilot a season table; **third person for every reader** (PROVISIONAL — see the flag below; Subject type keeps name/pronoun/possessive swappable and all verbs are past tense so second person is a parameter change); **two movements + cohort close** (own motion leads, connective tissue second, one collective clause off the window's event counts closes — George's "best of both worlds" call: accomplishments first, connection as the icing). Quiet-week fold-in as ratified: no own motion still narrates landing past work ("their own board sat quiet this week; their fingerprints didn't"), nothing at all gets "A quiet week — nothing recorded.", never silence, never scolding. Build-level interpretations: whistle_blown and ball_dropped are excluded from BOTH movements (a personal narrative must not keep score of stillness; only the pickup is witnessed); variant openers per week-shape picked by a deterministic seed (memberId, chapterIndex) so a page never rewrites itself on refresh; every clause is verb-led so it reads after any subject; chapters start at the member's arrival, not the season's. Home: /members/[id], "the season" section above assist history.**
>
> **WHISTLE DONE (2026-07-16, commit `066b303`, verified locally end-to-end via fixture users driving the real forms — 38/38 checks, fixtures deleted — and live on prod, all 6 routes 200):** three structural calls locked in serial (A/B/C): clock = **48h of no recorded motion** (project edits, task moves, or events — not events alone, so private task motion counts as motion); **public whistle, private cause** (one actorless feed line in the drop's language; the one-tap cause never reaches the events table; member pages never narrate it); **hybrid remedies** (define-it and link-the-evidence are one field inline on the /me pickup card like the catch; split-it and the two help causes navigate to the real forms with the whistle standing until the artifact exists — split-it even pre-opens the new-task form). Migration 0005 (whistle_cause enum, projects.whistle_blown_at/whistle_cause, event kinds whistle_blown + ball_picked_up), applied via direct SQL per the standing warning. Build-level interpretations recorded: only HELD balls whistle (an in-air pass has its own clock — the drop — and a drop resets the whistle window); a standing whistle never re-fires; one line per move — `ball_picked_up` is emitted only when the clearing artifact has no feed line of its own (inline remedies, split-it task creation, ball-panel edit), while blocker remedies and passes clear silently behind `blocker_raised`/`ball_passed`; createTaskAction now requires auth (attribution forces authentication, same surfacing as moveTaskAction). Poetic justice, kept: the sweep's first live act was whistling George's own "Whose Ball v2" project (ball held by George, still since Jul 13) — the pickup card is waiting on his /me, and it's PR-body material. Also observed live: the four fake demo PROJECTS are already gone; what remains of the fakes is 4 users (Maya Chen, Devon Okafor, Priya Raman, Sam Whitfield) + their 4 `submission_merged` feed events — the roster-ingest open item is now a user+event swap, not a project purge.**
>
> **RALLY DONE (2026-07-15, commit c813af9, verified locally end-to-end via fixture users driving the real forms, since deleted):** pass = giving the ball to someone else via set-ball (starts the in-air clock, `ball_passed` event); catch = one field on /me's catch card or the project ball panel, named first action becomes the project's nextAction, rally_count++, `ball_caught` carries elapsed_s (migration 0004: 3 enum values + events.elapsed_s + projects ball_passed_at/ball_passer_id/rally_count, applied via direct SQL). Drop = lazy sweep (sweepDrops on /me, /momentum, project page loads; no cron): rally resets, ball returns to the passer, `ball_dropped` renders ACTORLESS in the feed ("the ball dropped — 24 hours in the air, no catch"). Verified: steal attempt by non-receiver no-ops; holder-unchanged edit emits no pass; drop fired on a 25h-backdated pass; tiles (longest live rally, median catch this week) both live. Build-level interpretations recorded: cause-typed = the drop event carries a typed cause (only `uncaught for 24h` shipped; richer causes stay with the parked whistle); a catch attempted past 24h registers the drop instead. NOT YET ON PROD — push + prod smoke is the next block's first move (Friday trigger needs "verified on prod").
>
> **ASSISTS + POSSESSION DONE (2026-07-15, commit f4409f7, verified locally end-to-end via two fixture users driving the real forms, since deleted):** event kinds `assist` + `assist_converted` (migration 0003, applied via direct SQL per the standing warning). Assist logs to the named unblocker on any exit from blocked; converts once on ship (dedupe verified: done→building→done does not double-convert); when the unblocker moves the task themselves the assist line replaces blocker_cleared (one line per person per transition). New /members/[id] page: assists + conversions + narrated history, never task counts; momentum feed actor names link there (the weekly narrative's future home). Possession view replaced 'waiting on you' on /me: blockers naming you + balls you hold on others' projects, top three by wait time, always rendered, empty state verbatim "Nothing waiting on you. Clean hands."
>
> **JOURNEY HEADER DONE (2026-07-15, commit 75dc775, verified locally end-to-end):** `lib/journey.ts` (three phases off REVIEW_WEEK: Build → Review & vote → Operate) + journey strip rendered inside SiteHeader (so every page with the scoreboard bar has it; the stranger landing keeps its one-breath pitch) + full `JourneySpine` on /momentum only. Standing is phase-aware: Build = your submission not-in / saved-unmerged / merged ✓ (all three states driven and observed via a throwaway credentials user, since deleted); Review = `reviews x/N · votes x/N` in one left-join query (logic in place, not yet live-datable — phase flips Sun 17:00 ET). Countdown is a client component seeded by server time (`requestNowMs`), ticks 1s, amber under 24h. No migration needed. Microcopy plain per the ratified voice frame.
>
> **MOMENTUM DONE (2026-07-13, commit 28636a9, verified on prod):** append-only `events` table (enum of 7 kinds; votes deliberately never emit) written by the server actions at real transitions; /momentum = 5 collective tiles + shipping feed, no per-person counts anywhere; backfilled 7 historical events from state-table timestamps; v1 heartbeat front page + Redis/Anthropic/cohort-json modules retired (git history keeps them); front door now: signed-in → /me, stranger → landing; moveTaskAction gained the auth check attribution forced. NOTE: drizzle-kit push wanted to truncate reviews (diff confusion on the live unique constraint) — migration 0002 was applied by running its SQL directly; use that path for future migrations too.
>
> **DEPLOY DONE + VERIFIED (2026-07-13):** live at https://whose-ball.vercel.app — Vercel project `whose-ball` (team gge513-8058s-projects), Git-connected to gge513/whose-ball (every push to main auto-deploys). Env vars on Vercel (Sensitive, Production+Preview): DATABASE_URL, GITHUB_PAT, AUTH_SECRET (fresh prod-only value), AUTH_GITHUB_ID/SECRET from the NEW prod OAuth app "whose-ball (production)" on gge513 (one OAuth app per environment; callback https://whose-ball.vercel.app/api/auth/callback/github). Verified: all 7 routes 200, both providers live, George's GitHub sign-in round-trips to his /me with Neon data intact across 3 deploys. George's earlier drag-and-drop attempt ("Vercel Drop") errored and is superseded by the Git pipeline.
>
> Sessions A, B, C COMPLETE; ADMITTED; **DATES CONFIRMED from the enrolled dashboard (committed `f5e34d4`): the pilot started Mon Jul 13 (kickoff Zoom 6pm ET that night); submission PR merged by SUN JUL 19 17:00 ET into `rogerSuperBuilderAlpha/hult-cohort-program` (branch `participants/summer26/phase-1-project-1/gge513`, title `[Project 1] Submission — gge513`); 58 reviews + 58 votes by Mon Jul 20 14:00 ET (cohort 59).** Week plan: Session D lands Tue/Wed; Thu/Fri polish + fresh-clone verify; PR opens Sat, merged well before Sun 17:00. Built: schema on Neon (6 tables), task board with Define gate/ball/blockers, two-door auth, /tasks filters, /me command center, /review console (review-gated private ballot, pace panel, deep flags, GitHub detection, gate verified end-to-end by George), v1 vote console retired. Demo peers are FAKE seeded rows — replace with the real roster or label before any demo/submission. **Next:** Session D — momentum page (collective tiles + shipping feed, no rank), journey header, per-member weekly narrative off merged PRs (rework of the v1 heartbeat front page), README from THESIS.md + AGENTS.md, deploy to Vercel (needs prod OAuth callback + env vars from George), 10-user smoke. Do not re-derive; resume there.
>
> **FAKE-ROW DELETE DONE (2026-07-18, George's call: delete his three rows, keep the peers):** surgically deleted submission id 1 (`pm-gge513`) + events id 3 (`review_filed`) and id 4 (`submission_merged`). Pre-checked: no reviews or votes referenced submission 1, so no FK fallout. Verified live on prod: /members/1 narrates only real work (no submission claim), his /momentum lines are `started Whose Ball v2` + the shipped server-actions task, remaining merged-PR feed lines all belong to the four kept demo peers. Kept deliberately (outside the approved scope, part of the roster-swap open item): George's fake `reviews` row (of Maya's submission 2) + fake `votes` row on the same, private review-console demo state against the kept peers, not public narrative. Roster ingest now swaps: 4 fake users + their 4 `submission_merged` events + George's fake review + fake vote.
>
> **Session D open items (beyond the build itself):** sign the Expectations Acknowledgment on the dashboard (control not yet located — likely post-kickoff); pull the cohort MCP server guide (dashboard references programmatic access to reviews/votes — potential console integration); update `lib/github-reviews.ts` title matching to the live format `Review by @{me}: @{peer}`; ingest the real roster once the submissions branch fills and DELETE the four fake demo peers; note anything the kickoff call changes.
>
> **PROVISIONAL — review after George's real live test (flagged 2026-07-16):** weekly-narrative narrator stance locked as third-person-for-every-reader (the witness stance) so the build can move, but the reasoning didn't fully land for George. Revisit with his own page in front of him after he's used it live: does reading your own week in the third person feel like being seen, or like being talked about? If it's the latter, the fallback is second-person-on-your-own-page, and the sentence rules should be written so the subject is swappable without restructuring.
>
> **Parked for a future build session (George, 2026-07-13):** Session C flex hardening — paste-path existence verification via the GitHub API, page-load detection sweep across all peers, review-quality signals (150-word/citation checks on issue bodies), staff-only tally export for after votes close.

## Framework harvest (2026-07-13, all six ratified by George in serial walk-through)

Source ore: `docs/brainstorms/2026-07-13-motivation-framework-ore.md`. Thesis: `docs/THESIS.md`. Filter applied throughout: a delta earns core scope only if a stranger feels it in the first 30 seconds of a smoke test, and reflective input is never required to act.

1. **Pipeline at project altitude.** Projects carry a six-stage trajectory (Define → Commit → Build → Verify → Ship → Teach); tasks ride a light flow (todo / building / verifying / done, clears the 3-state baseline). Chosen over per-task six stages (ceremony) and skippable stations (decoration).
2. **The ball is a button.** Every project has one first-class next-action field and a holder; rendered on the command center as a click-through into where the work lives (task, PR, review issue). When-commitments optional, expire quietly. George's own restart mechanism ("click to action with the next step defined in front of me"), the cohort version of his /resume.
3. **Blockers, light version.** "Blocked" is a task state requiring three fields (what I tried, what I need, who can unblock). Surfaces on the momentum board and the unblocker's dashboard. Full help-matching system goes on the pinned public roadmap (which the operator handbook requires anyway).
4. **Front door = personal command center; momentum one glance away.** Cohort-pulse strip on the dashboard (deployed / PRs merged / reviews filed / help requests) clicking through to the full Momentum page. v1's who-shipped-who-is-quiet grid is retired: movement stays public via a shipping feed of real events; comparison and ranking do not exist anywhere. Per-person GitHub-fed weekly narrative survives on each member's own page.
5. **Define gates the story, never the work.** Instant name-only project creation; the three meaning questions (who benefits, what changes, what does done look like) are answered to advance out of Define; tasks are never blocked by stage; moving-but-undefined projects get a nudge. Staff smoke-test path is untouched by the gate.
6. **Thesis now.** One-pager assembled strictly from ore + these decisions; triple-use as README opening, demo-video spine, and pitch source. Stamped provisional; revisited once at ship.

Parked in ore (operator-phase, post-win): cohort failure-mode interviews, the hypothesis table, the metrics program.

## Second harvest (2026-07-15, all ratified by George in serial walk-through)

Source ore: `docs/brainstorms/2026-07-15-evernote-pm-communication-ore.md` (mined from George's Evernote: PM struggles + communication breakdowns). Same 30-second-smoke-test filter applied.

1. **The Rally (signature mechanic).** A pass puts the ball in the air; it becomes the receiver's only when they catch it by naming their first action (one field). Uncaught after 24h = a visible, no-fault, cause-typed drop on the project. Projects carry a rally count of consecutive clean catches; momentum page shows the cohort's longest live rally as a collective tile. Never a per-person stat. Early-catch reward is witness, not points: the feed states catch latency in its event language ("caught in 90 minutes") and momentum gets a "median catch time this week" tile, both display-only on timestamps the catch event carries. PARKED: per-project "hot rally" visual state (badge-adjacent, threshold unknowable pre-pilot).
2. **Assists.** When a blocker naming you as unblocker clears, an assist event logs to you; when the unblocked task ships, the assist converts. Member pages show assists + conversions; the feed narrates them. Farming accepted as low-risk (conversions require a real ship).
3. **Dead-ball whistle with cause-typed pickup: PARKED with a Friday trigger.** If Rally and Assists are landed and verified on prod by Friday Jul 17 morning, pull back into scope; otherwise it leads the pinned public roadmap. Never ship whistle-plus-cause without the routed remedies.
4. **Possession view.** Strip on /me: balls you hold that others are waiting on, capped at top three, ranked by wait time, private, never scored. Empty state: "Nothing waiting on you. Clean hands."

**Voice frame (ratified same day, B/B/A):** the DFW-informed voice lives in the weekly narrative and momentum page only, mechanic microcopy stays plain; vocabulary is only the words the mechanics earn (rally, catch, drop, assist, hold), no decorative tennis lexicon; DFW is named and quoted, one real epigraph in the README/thesis (candidate: the Federer-essay line on beauty and high-level sport). Wallace-informed, never Wallace-imitating. The mapping (Joyce essay → Assists; Federer Moments → feed-without-rank; chess-on-the-run → ball-as-button; the rally as the living unit → catch-or-drop) is the PR body's motivation-design-notes spine.

## Live-site re-check (2026-07-18, supersedes the 2026-07-13 reconciliation where they differ)

Source: the public program page, fetched Fri Jul 18 while answering "how do I review the merged submissions."

| Fact | Live site 2026-07-18 (authoritative) | Previously verified (Jul 13) |
| --- | --- | --- |
| Cohort size | **65 enrolled → 64 peer reviews** | 59 → 58 reviews |
| Review window | **opens Sun Jul 19 21:00 UTC (17:00 ET, at submission close) · closes Mon Jul 20 18:00 UTC (14:00 ET)** — a 21-hour window | close time known, open time not |
| Vote mechanics | **PUBLIC opt-in upvote: "Vote: up" written inside the review issue, or abstain; most votes wins** | private review-gated thumbs up/down, no tallies (winner-selection.md) |
| Review artifact | "Written technical review on each peer submission (GitHub issue)"; filing LOCATION still unstated — repo doc says reviewee's repo, the two-handle title `Review by @{me}: @{peer}` and Roger's own example issues on the cohort repo suggest central filing. **CONFIRM ON THE ENROLLED DASHBOARD before filing the first one.** | title format from the dashboard |

Implication for whose-ball: the app's private-ballot review console implements the Jul 13 governance spec, which the live site has since changed (public Vote: up in-issue). Treat as a known limitation + candidate post-merge update, alongside the `github-reviews.ts` title-format fix. Implication for George's own duty: up to 64 reviews in a 21-hour window is only survivable with prep — draft reviews against already-merged peers BEFORE the window opens (13 unique peers merged as of Fri evening), file when it opens.

## Live-site reconciliation (2026-07-13, superseded above where they differ; supersedes repo docs where they differ)

Source: https://site-nine-rouge-68.vercel.app/program/phase-1-project-1 (the platform George was admitted to). Where this section conflicts with the repo-doc facts below, THIS wins.

| Fact | Live site (authoritative) | Old assumption (repo docs) |
| --- | --- | --- |
| Cohort size | **56 enrolled → 55 reviews + 55 votes** per Phase 1 project | 30 → 29 reviews |
| Submission | **PR to `rogerSuperBuilderAlpha/hult-cohort-program`**, branch `participants/summer26/phase-1-project-1/{handle}` → target `projects/summer26/phase-1-project-1`, title `[Project 1] Submission — {handle}` | repo `pm-{handle}` in a cohort org |
| Deploy deadline | **PR merged by Sunday week 1, 17:00 ET** (unmerged = ineligible) | Thu 17:00 |
| Review window closes | **Monday week 2, 14:00 ET** (no Wed checkpoint mentioned) | Wed 14:00 ≥10, Fri 14:00 all |
| Review issue title | **`Review by @{me}: @{peer}`** (two handles) | `Review by @{reviewer}` |
| PR body must include | Production URL · fresh-clone setup · architecture · **motivation/engagement design notes** · known limitations · agent usage | README-centric |
| Judging emphasis | **"Motivation is the key variable... progress visibility, clear next actions, signals that make people want to ship"** | 5-dimension rubric |
| Prerequisites | Cursor + Claude Code active (~$400/mo); **Expectations Acknowledgment signed on the dashboard** | — |

Implications for the build: pace math and seed scale should reflect ~55 peers; `lib/github-reviews.ts` title matching must accept the `Review by @{me}: @{peer}` format; `lib/review-week.ts` gets real dates once the dashboard shows them; the PR body's "motivation / engagement design notes" section is THESIS.md's job. Absolute calendar dates for week 1 are still unknown — read them off George's signed-in dashboard. The page also ships an "agent prompt template" for submission day; we use our own flow but its branch/title/body requirements are the contract.

## Verified sources (all in the public program repo)

| Fact | Source file |
| --- | --- |
| Baseline features, production bar, ballot eligibility | `curriculum/phase-1/project-1-pm-platform/requirements.md` |
| Review rubric (5 dimensions, weights, per-review time budget) | `.../review-rubric.md` |
| Review system (29 reviews, GitHub-issue format, quality grading) | `assessment/peer-review-system.md` |
| Vote mechanics (private thumbs, review-gated, no tallies) | `governance/winner-selection.md` |
| The weekly loop + pilot deadlines | `curriculum/phase-1/the-loop.md` |
| Winner's operator obligations (SLAs, integrations) | `.../operator-handbook.md` |

Local snapshots of all six are in the session scratchpad; re-pull from GitHub if stale.

## The headline: whose-ball as built is ineligible

Ballot eligibility requires ALL baseline features. Whose-ball has none of them; it was designed for a different contest brief ("don't rebuild a task tool, track shipping") that does not apply here.

**Baseline (required for ballot) vs current state:**

| Requirement | Whose-ball today |
| --- | --- |
| Projects: create/edit/archive | Missing |
| Tasks: title, description, status, assignee | Missing |
| Status workflow, 3+ states | Missing |
| Assign task to any cohort member | Missing |
| Multi-user auth, 30+ accounts (email+password or OAuth) | Partial: GitHub OAuth only; staff test account is email+password (`staff-review@hult-cohort.test`), so a credentials path is needed |
| Task list views: filter by assignee / status / project | Missing |
| Public HTTPS deploy, data persists across redeploys | Not deployed; Redis persistence exists |

**What survives as differentiators (explicitly listed in requirements.md as rubric-scored):**

- GitHub integration (link task to issue/PR): the heartbeat's whole engine.
- Review/vote module ("strongly valued"): our vote console, rebuilt to the real mechanics.
- Metrics dashboard ("PR counts, review completion"): heartbeat + review tracker, verbatim.
- Notifications, due dates, mobile responsiveness: candidates if time allows.

So the v2 shape: **a real task board at the core (eligibility), with whose-ball's GitHub-native intelligence as the differentiating layer (the win).** The name still works: the review queue is 29 balls in your court.

## Verified vote mechanics (replaces the approval-voting console)

1. Reviewer files a written review as a GitHub Issue titled `Review by @{handle}` on the reviewee's repo (150+ words, 3+ file citations, 5-dimension rubric, one actionable suggestion).
2. Saving that issue URL on the platform **unlocks** the vote for that peer.
3. Vote is a **private thumbs up / thumbs down per peer**. Individual votes private, live tallies never shown during review week, aggregates staff-only until announcement.
4. No self-review, no self-vote (UI + API enforced). Winner = most thumbs up; tie-break = median total rubric score.

Current console violates every axis (public counts, optimistic approval voting, self-vote allowed). Rebuild, don't patch.

## The review week is the killer feature surface

Scale at cohort 30: 29 reviews per person per project, ~45 min each, ~22 hours in one week. Milestones: 10 reviews due Wed 14:00, all 29 due Fri 14:00, votes Fri 16:00. Three staff-assigned deep reviews (300+ words) among them. Reviews are graded; short, citation-free, or copy-paste reviews hurt the reviewer.

The review console therefore tracks, per user: reviews filed (read live from GitHub Issues authored by them titled `Review by @`), which are deep-review assignments, words/citations sanity flags, pace vs the Wed/Fri lines, and the review-gated private ballot. This is simultaneously the cohort's biggest pain point and a named rubric differentiator.

## Journey spine (verified loop)

Build (Tue kickoff, Thu 17:00 deploy) → Review (Thu 17:00 to Fri 14:00) → Vote (Fri 16:00) → Operate (winner, with SLAs: 99% uptime, 24h PR triage, releases every 2 weeks). Everyone else: 2 PRs + 1 issue + 3 PR reviews per cycle on the winning platforms. The spine renders the current phase, its deadline, and the user's standing against it.

**Dates, provisional:** the-loop.md's compressed pilot table says Project 1 deploys **Thu Jul 22, 17:00 ET** (then Jul 29, Aug 5 for projects 2 and 3), while requirements.md carries an Oct 2 date from the fall term. If the Jul 22 date governs George's cohort, the build window after admission is days, not weeks, which is exactly why we are pre-building now. Confirm at kickoff.

## Repo constraints to honor at submission

- Repo will live in the cohort org as `pm-{github-handle}`, public, MIT, from the org template. Whose-ball code ports into it; this folder remains the workshop.
- `AGENTS.md` required. README must include setup, architecture diagram (ASCII fine), deploy URL, known bugs.
- Submission is a PR merged to the cohort repo's `main` by the deploy deadline; the PR body is the proof-of-work record. Staff smoke-test the URL at deadline; down = ineligible.
- Signup instructions in README; 5+ reviewer accounts on request; staff test account creatable.
- No hardcoded secrets; 10 concurrent users without visible errors.

## v2 build sequence (teaching sessions, in order)

- **Session A — the task board core.** Data model (users, projects, tasks) in Postgres (Neon via Vercel marketplace, Drizzle ORM; recommendation, ratify at session start). Projects: create instantly (name-only), edit, archive-flag, six-stage `stage` enum with the Define meaning fields (who benefits / what changes / done looks like) gating advancement only. Tasks: title, description, 4-state `status` enum (todo / building / verifying / done) plus `blocked` with its three fields, assignee FK, optional definition-of-done, `next_action` + holder on projects. Teaching: schemas, relations, enums as loud-failure design, CRUD, server actions.
- **Session B — people + the command center.** Email+password (credentials + hash) alongside the existing GitHub OAuth; open registration; assignment to any member; filter views by assignee/status/project (baseline). Personal command center front door: your balls as click-to-action buttons, who is waiting on you, cohort-pulse strip. Teaching: auth, sessions, authorization vs authentication.
- **Session C — the review console + real vote.** GitHub Issues ingestion for `Review by @` (the heartbeat pattern re-aimed), pace tracking vs Wed/Fri lines, deep-review flags, review-gated private thumbs with no visible tallies and no self-vote. Teaching: third-party API ingestion, invariants enforced server-side.
- **Session D — momentum + journey + ship.** Momentum page (collective tiles + shipping feed; no ranking), phase-aware journey header, per-member weekly narrative off merged-PR activity, README opening from THESIS.md + AGENTS.md, deploy, 10-user smoke, load-test flex.

Each session ends by moving this cursor and appending a COURSE-LOG entry with its flashcard deck.
